package com.idan.teamusup.data;

public enum Constants {
    ADMIN,

    bundle,

    instances,
    isNew,

    lat,
    lng,
    level,
    location,

    name,

    photoUrl,
    playersSize,

    totalGames,
    topScorer,
    time,
    teamSize,
    teamsSize,
    timeSize,

    user
}
